name: useTf
channels:
   - defaults
dependencies:
   # Core requirements
   - python=3.8
   - pip
   # Other packages
   - pandas
   - numpy
   - matplotlib
   - tensorflow
   - seaborn
   # pip install
   - pip:
     - emcee
     - corner
     - tqdm